spring.application.name=shooter-server

# הגדרות חיבור ל-Supabase PostgreSQL
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-0-eu-central-1.pooler.supabase.com:5432/postgres?user=postgres.bbybpeunbofshmscofxn&password=הסיסמה_החזקה_שלכם}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DB_USERNAME:postgres.bbybpeunbofshmscofxn}
spring.datasource.password=${DB_PASSWORD:yxkgvfkXMWT09faR}

# הגדרות Hibernate עבור PostgreSQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# הגדרות אפליקציה וטוקנים (JWT)
deadzone.jwt.secret=${JWT_SECRET:deadzone-development-secret-change-before-production}
deadzone.app.base-url=${APP_BASE_URL:http://localhost:5173}
deadzone.email.from=${MAIL_FROM:edurace2026@gmail.com}

# הגדרות שרת המייל (SMTP) שלכם
spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:edurace2026@gmail.com}
spring.mail.password=${SMTP_PASSWORD:eccjrwkmwkvlcydu}
spring.mail.properties.mail.smtp.auth=${SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SMTP_STARTTLS:true}
spring.mail.properties.mail.smtp.starttls.required=${SMTP_STARTTLS_REQUIRED:true}
spring.mail.properties.mail.smtp.connectiontimeout=${SMTP_CONNECTION_TIMEOUT:10000}
spring.mail.properties.mail.smtp.timeout=${SMTP_TIMEOUT:10000}
spring.mail.properties.mail.smtp.writetimeout=${SMTP_WRITE_TIMEOUT:10000}